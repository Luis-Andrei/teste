{
  "name": "lancheria-3-alianca",
  "version": "1.0.0",
  "description": "Site da Lancheria 3 Aliança",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prod": "NODE_ENV=production node server.js",
    "restart": "pm2 restart dashboard",
    "stop": "pm2 stop dashboard",
    "logs": "pm2 logs dashboard",
    "update": "npm install && pm2 restart dashboard"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pm2": "^5.3.1",
    "compression": "^1.7.4",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 